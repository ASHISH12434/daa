#include <stdio.h>
int main() {
    int a[5]={4,2,7,1,9}, n=5, x=7, i, f=0;
    for(i=0;i<n;i++)
        if(a[i]==x) { f=1; break; }
    if(f) printf("Found at %d", i);
    else printf("Not found");
    return 0;
}
